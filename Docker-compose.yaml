services:
  stag_io:
    build:
      context: .
      args:
        - DEV=true
    ports:
      - "8000:8000"
    volumes:
      - ./stag_io:/stag_io
    user: root
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
